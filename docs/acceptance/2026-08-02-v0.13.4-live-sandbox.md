# v0.13.4 Live Sandbox Acceptance

This repository-safe record captures aggregate evidence from the August 2, 2026 UTC enterprise-preview acceptance.
Tenant URLs, user identities, Credential Vault IDs, Forward network/snapshot IDs, dependency rows, endpoints, and raw
API responses remain in the protected operator record.

## Decision

**Read Only on-demand sandbox acceptance: PASS with production blockers.** The app and Workflow completed a current
telemetry-to-Forward plan without mutation. Production promotion, recurring scheduling, and Network Admin apply were
not approved.

## Runtime Evidence

| Control | Result |
| --- | --- |
| App identity and version | `my.forward` `0.13.4`; unsigned enterprise preview; registry resources ready |
| Installed archive SHA-256 | `ce7d9ad07f31c02b1eb33bf543e9213275f9bf7c56317dad66580a7e4c8156b0` |
| Widget compatibility | Both toolkit and hosted Workflows paths returned the widget; the live task rendered the connection picker and plan editor without a JavaScript syntax error |
| Workflow state | Live, on demand, plan-only; no recurring schedule |
| Workflow execution | `c1fad27b-3987-47a5-be41-187da510c3f2`; `SUCCESS`; 11 seconds |
| Grail query | `019fbff3-4c89-7c30-9a83-56142675bd59`; 1,000 current results; query window `now()-15m` |
| Evidence source | Current client/server spans labeled `instrumented-transaction`; generated lab traffic traversed the running containerlab environment and OTLP collector; not production customer traffic and not a static/replayed Workflow fixture |
| Host resolution | 1,000 total; 1,000 ready; zero review, mapping, ambiguity, or unresolved rows |
| Modeled paths | 1,000 total; 1,000 queryable; 1,000 reachable; zero failed, blocked, unmapped, or ambiguous rows |
| Reconciliation | 1,000 unchanged; zero create, changed, stale, or collision rows |
| Operation boundary | `plan`; `tenant-managed-secret-backend-only`; zero mutation requests |
| Package ID | `dynatrace-forward-20260802005027726-04b785596cb8` |
| Plan digest | `91c000a4b2fccda9b183b2fe9a1646082e1259e22ba7bb434c84bc3ca0e46228` |

At evidence capture time, all 160 nodes in the `enterprise-prepost-scale` containerlab were running, the Dynatrace
OpenTelemetry collector was running, and the Forward cloud collector was running. Those runtime checks establish the
live sandbox provenance; they are not release-artifact tests.

## Secret And Identity Findings

- The Forward credential is stored in an AppEngine-scoped Dynatrace Credential Vault entry. Owner-only user access is
  enabled, ad hoc/no-app-context access is off, and external-vault synchronization is off.
- The tenant's **Dynatrace apps with access** selector did not offer either `my.forward` or `Forward`; only **All
  applications** was available. The unsaved restriction probe was discarded to preserve the working connection. This
  remains a production least-privilege blocker.
- The current Forward API token resolves to a human-backed local identity with organization-administrator privilege,
  not an integration-named least-privilege service identity. A read-only API attempt to enumerate administrator users
  was denied with HTTP 403. A Forward administrator must provision the dedicated service identity and rotate the Vault
  secret before production.

## Explicitly Not Done

- No Workflow schedule was created.
- No Network Admin connection or apply request was enabled.
- No Forward checks were created, updated, or deleted.
- No production customer telemetry was claimed.
- No signed `com.forward.dynatrace` distribution was claimed.

The next production-readiness review must require the dedicated Forward service identity, exact-app Vault scope or an
approved tenant fix, signed distribution, customer-owned telemetry mapping, operating cadence, and write-approval and
rollback ownership.
